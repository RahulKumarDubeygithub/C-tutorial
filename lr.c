#include<stdio.h>
int linear_searchR( int arr[],int size,int key)
{
    if(size<0)
    {
        return -1;

    }
    if(n>=0)
    {
        return linear_searchR(arr,size-1,key);
    }
}
int main()
{
    arr[100],key,index;
    printf("enter element of array:\n");
    scanf("%d",&arr[]);
    int index = linear_searchR(arr,,key);
    if ()
    {
        
    }
    
}